max_iterations: 80
checkpoint_interval: 25

llm:
  primary_model: "qwen/qwen3.7-max"
  primary_model_weight: 1.0
  api_base: "https://routerai.ru/api/v1"
  temperature: 0.7
  max_tokens: 8000
  timeout: 120

prompt:
  system_message: |
    See evolution_system_message.md — loaded at runtime.

database:
  population_size: 40
  archive_size: 20
  num_islands: 1
  elite_selection_ratio: 0.2
  exploitation_ratio: 0.7
  similarity_threshold: 0.995

evaluator:
  timeout: 3600
  parallel_evaluations: 1
  cascade_evaluation: true
  cascade_thresholds: [1.75, 0.0]

diff_based_evolution: true
max_code_length: 60000
# Keep one long-lived worker; FLUX stays loaded (no process recycle)
max_tasks_per_child: 1000

early_stopping_patience: 30
convergence_threshold: 0.05
early_stopping_metric: alignment_score
